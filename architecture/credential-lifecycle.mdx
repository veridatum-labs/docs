---
title: "Credential Lifecycle"
description: "How EarnProof credentials are created, verified, expired, and revoked"
---

EarnProof credentials are signed claim payloads generated from qualifying Stellar payment activity.

## Creation

1. Worker authenticates with a Stellar wallet.
2. Backend indexes incoming payments.
3. Worker classifies qualifying income payments.
4. Worker selects a proof type and condition.
5. Backend evaluates the condition.
6. Backend creates a canonical credential payload.
7. Backend signs the credential.
8. Optional proof commitment is registered on Soroban.

## Credential Shape

```json
{
  "id": "proof_01JXYZ123",
  "version": "1.0",
  "type": "minimum_income",
  "issuer": {
    "id": "issuer_earnproof",
    "name": "EarnProof",
    "publicKey": "G..."
  },
  "subject": {
    "walletHash": "sha256:..."
  },
  "network": "stellar-testnet",
  "asset": {
    "code": "USDC",
    "issuer": "G..."
  },
  "claim": {
    "operator": "greater_than_or_equal",
    "threshold": "1000",
    "frequency": "monthly",
    "satisfied": true
  },
  "disclosure": {
    "exactAmountRevealed": false,
    "sourceTransactionsRevealed": false,
    "senderRevealed": false
  },
  "expiresAt": "2026-08-13T12:00:00Z",
  "commitment": "0x...",
  "signature": "..."
}
```

## Verification

The verifier checks:

- schema version,
- signature,
- expiration,
- revocation,
- issuer status,
- optional on-chain commitment,
- disclosed claim result.

## Expiration

Every proof must include an expiration. Expired proofs remain historically visible but should not be treated as currently valid.

## Revocation

Proofs can be revoked by the worker or authorized issuer flow. Revocation should update backend status immediately and update Soroban status when the proof has an on-chain commitment.

## Portability

Future versions should support portable credentials that can be verified outside the EarnProof web application using SDK helpers and formal protocol schemas.

